# Shared defaults - loaded for all environments
AWS_PROFILE=base-profile
AWS_USER=your-user@example.com
AWS_PORT=53360
# KAFKA_PATH defaults to <project_root>/devkit/kafka — override only if needed
CLUSTER_DOMAIN=yourdomain.com
